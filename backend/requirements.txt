# Runtime dependencies actually imported by this project.
# The previous file was a UTF-16 encoded pip freeze of a whole dev machine:
# pip cannot read UTF-16, so installs failed outright, and it pulled in
# torch, ultralytics, opencv, catboost and ~100 other unused packages.
# The complete original list is kept in requirements.freeze.txt.

Django==6.0.1
djangorestframework==3.17.1
django-cors-headers==4.9.0
firebase-admin==7.4.0
gunicorn==26.0.0
